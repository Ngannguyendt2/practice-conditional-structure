function areaTriangle() {
    let num1=Number(prompt("Please import the first edge length"));
    let num2=Number(prompt("Please import the second edge length"));
    let area=(num1*num2)/2;
    alert('Area of Triangle is '+ area);

}